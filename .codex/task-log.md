# 任務工作紀錄

> 用途：持續記錄本任務每日完成的工作，供每日總結報告使用。

## 2026-07-29

- 建立持續工作紀錄規則：記錄已完成事項、修改檔案、驗證結果、待辦、阻塞與風險。
- 目前尚未執行具體程式碼修改。
- 確認租房流程需求：前 3 項合併為「確認建案、業主、房產已存在」，最後新增「流程結束」，共 14 個步驟。
- 完成並記錄設計規格，待使用者審閱後進入實作。
- 實作拆解時修正第 13 步與「流程結束」的邏輯：日常營運以房產進入營運階段判定完成，避免持續性工作讓結束步驟永遠無法完成。
- 完成租房流程管理實作：新增 14 步純流程判定、房產相關資料載入、委託文件／帳單／收款／營運資料串接，以及三語步驟文案。
- 新增測試：流程順序與結束守門 2 項、流程頁整合 1 項、三語文案 1 項；聚焦測試結果 4/4 通過。
- 前端 build 通過（Vite exit 0）；保留既有 chunk size warning。
- 前端完整測試結果：44 項中 38 項通過、6 項既有失敗，失敗集中於報表檔名、預備金工具列、租客列表與合約模板，未涉及本次租房流程檔案。
- 完成租房流程分頁調整：每頁垂直顯示 5 個步驟，共 3 頁（5/5/4）；預設跳到第一個未完成步驟所在頁，切換房產時重置分頁。
- 將流程主內容、時間軸與步驟卡片設為滿寬，新增上一頁／下一頁與頁碼控制，並補充中英文分頁文案。
- 新增分頁純函式測試，租房流程聚焦測試 7/7 通過；前端 Vite build 通過。
- 嘗試使用瀏覽器進行視覺檢查，但本地頁面因未登入導向 `/admin/login`，因此未能驗證登入後畫面；目前以測試與 build 結果完成程式驗證。
- 使用者回饋流程列表仍未填滿；定位原因為 `.process-center-main` 的左右 `padding:20px` 留下內縮空白。
- 修正主流程面板為左右滿寬，新增滿寬回歸測試；租房流程相關測試 8/8 通過，Vite build 通過。
- 使用者提供截圖確認真正的留白在左側「選擇房產」列表；修正列表穿透選房欄左右內距，填滿整個選房欄寬度。
- 新增房產列表滿寬回歸測試，相關測試 4/4 通過，Vite build 通過。
- 使用者回報右側空白仍存在；再次定位為 grid 與捲軸共同作用下的右內距，改用取消選房欄右內距、列表補足 16px 的穩定版面規則。
- 第二版滿寬回歸測試 4/4 通過，Vite build 通過，待使用者刷新畫面確認。
- 使用者再次提供截圖，確認左側選房欄仍有右側內縮，並要求移除舊的「房產流程／流程中心」通用資料頁。
- 將選房欄改為無水平內距，列表直接使用整個欄寬；DataWorkspace 遇到 adminProcess 時改渲染新的租房流程中心，舊列表／看板／時間軸頁不再顯示。
- 新增舊流程頁移除回歸測試；租房流程相關測試 10/10 通過，Vite build 通過。
- 使用者再次要求直接刪除下方舊流程；移除房產詳情頁舊流程模板、計算屬性與操作方法，避免舊區塊以任何入口重新出現。
- 最終租房流程與舊流程移除測試 11/11 通過，Vite build 通過。
- 使用者再次回饋截圖，確認左側選房欄仍有可視留白；重新分析為 grid 內容高度自動分配問題。
- 將選房欄改為固定標題／搜尋列＋列表填滿剩餘高度的三段布局，列表不再使用負 margin 或固定 max-height。
- 最終版面回歸與租房流程測試 11/11 通過，Vite build 通過。
- 使用者最新截圖確認同一個流程入口上下同時出現兩套界面；根因是 AdminPage 的流程中心分支與後續 `DataWorkspace v-else` 不在同一條互斥條件鏈，導致兩個租房流程中心同時渲染。
- 將房產詳情頁改為流程中心分支的 `v-else-if`，讓流程中心、房產詳情與通用資料頁只能渲染其中一套；新增重複渲染回歸測試。
- 同步完成左側選房欄自動收高與超出時才滾動的版面修正，避免列表下方保留大塊空白。
- 本次租房流程與界面回歸測試 12/12 通過，Vite build 通過（保留既有 chunk size warning）。
- 使用者提供最新截圖，要求在不需要整頁往下捲的情況下看到租房流程分頁框；定位為右側流程卡片把分頁推到視窗底部外。
- 將右側流程布局限制在視窗可視高度內，主流程區改為內部滾動，分頁列使用 sticky 固定在流程區底部；新增分頁可視性回歸測試。
- 本次租房流程與界面回歸測試 13/13 通過，Vite build 通過（保留既有 chunk size warning）。
- 使用者要求繼續把可操作步驟留在流程中心；補齊「出租準備」與「完成交房」的當前頁表單，包含單位編號、房型、樓棟、樓層、臥室數量、買入價與實際交房日期。
- 表單提交接入 `saveAdminPropertyWorkspaceBasic`，交房完成時更新房產為 `OPERATING`；保存後重新同步工作區房產資料，避免界面仍顯示舊的資產階段。
- 新增出租準備表單的簡體／繁體／英文文案與回歸檢查；租房流程相關測試 13/13 通過，Vite build 通過（保留既有 chunk size warning）。
- 修正「完成交房」在不同出租階段的處理：出租前仍保存房產基本資料並切換營運狀態；已有本次出租委託時，改為在當前頁保存交房日期、房況、鑰匙／門卡數量、水電表讀數、接收人與備註。
- 交房保存改用後端要求的 `completed: true` 欄位，補齊三語表單文案與回歸測試；租房流程相關測試 13/13 通過，Vite build 通過（保留既有 chunk size warning）。
- 使用者要求繼續收斂流程中心外跳；新增第四階段「建立首期帳單」的當前頁月份表單與提交動作。
- 租約建立本來會自動產生帳單，但對未生成或需要補建的月份缺少管理入口；新增 `POST /admin/tenancy/leases/{leaseId}/rent-invoices`，按當前租約區間計算日租／月租金額並防止重複帳單。
- 前端接入 `createAdminLeaseFirstInvoice`，並限制帳單月份落在當前租約期間；租房流程相關測試 13/13 通過，Vite build 通過（保留既有 chunk size warning）。
- 完成流程中心剩餘內嵌操作：交房抽屜可編輯／新增交接清單項目、上傳交接照片與房產附件，保存前要求至少有清單與照片，避免假完成。
- 新增按登入管理員角色限制主操作；非負責角色只能查看歷史，按鈕會禁用並顯示聯絡負責人的三語提示，ADMIN 或未配置細分角色時維持完整管理權限。
- 補齊相關三語文案、測試與實作計畫；租房流程相關測試 13/13 通過，Vite build 通過（保留既有 chunk size warning）。後端 Java 編譯因本機未安裝 Java 未能執行。
- 收尾時執行前端 31 個測試檔共 58 項；本次租房流程相關 13/13 通過，但完整套件仍有 8 項既有授權、報表、預備金、租客詳情與合約測試因目前工作區其他重構而失敗，未將其混入本次流程中心修改。
- 使用者回報 Spring Boot 編譯錯誤「实际参数列表和形式参数列表长度不同」；本地未安裝 Java/Maven，已先核對本輪新增帳單 DTO、Controller、Service、Mapper 的方法參數目前一致，待完整錯誤行號以建立精準編譯回饋。
- 已找到本機 JDK 17 與 Maven，取得依賴後執行 Spring Boot 編譯；第一次增量編譯通過但未重新編譯，`clean` 因 PDF 模板檔被占用而失敗，隨後執行全量 `mvn compile`，實際重新編譯 275 個 Java 源文件並成功，未重現參數列表錯誤。

- 使用者重新定義流程中心：由固定 14 步檢查表改為「本次出租」工作台；历史委托、旧授权书、旧租约不再参与当前完成度，收租／维修／预备金／提醒不再算入出租流程完成度。
- 建立 Rental Cycle 设计规格与实现计划：以一笔当前出租委托作为本次出租锚点，并为新租约增加 `rental_mandate_id` 关联；旧数据保持可查但不猜测回填。
- 将流程中心改为四个不分页阶段：出租准备、委托与授权、招租与签约、入住与首期收款；每阶段显示状态、负责人、缺少资料、阻塞原因、精准主操作与历史入口。
- 新增 `rentalCycleWorkbench` 纯模型与测试：历史委托会被排除，未完成业主授权签署会明确阻塞，当前租约首期收款完成后四阶段才会完成；日常运营明确不参与完成度。
- 完善三语工作台文案，加入“无法审核：请先生成并完成业主授权委托书线上签署”等阻塞原因，并统一四阶段、角色、缺失资料和主按钮文案。
- 当前租约工作区、OTR、租约合约、账单及收款跳转改为携带当前委托／租约 ID；租约建立弹窗会传递 `rentalMandateId`，OTR 路由可直接打开对应租约操作位置。
- 租约选项接口补充 `rentalMandateId`；可建立租约的单位资格改为与当前出租委托状态及有效期一致，避免委托页与租约页显示不一致。
- 移除流程中心不再使用的 14 步流程判定与分页纯模块，避免旧模型被后续功能重新引用。
- 验证结果：当前出租工作台聚焦测试 12/12 通过，Vite build 通过；build 保留既有 chunk size warning。后端 Maven 尚未执行，环境缺少 Java/JAVA_HOME。
- 使用者反馈流程中心文字贴近边框、阶段盒子之间距离过小；定位为主流程面板使用 `padding:20px 0`，阶段列表 `gap:8px`，阶段卡片内距仅 14px。
- 调整流程中心主面板为四周 20px 内距，当前任务盒子改为 18px 外间距／内距，阶段盒子间距改为 12px，卡片内距改为 16px 18px，并增加标题区与阶段列表之间的留白。
- 新增间距回归测试；流程中心界面测试 8/8 通过。
- 使用者确认希望尽量留在流程中心完成操作，而不是跳到其他模块；新增当前页操作抽屉第一阶段。
- 当前页已接通建立出租委托、生成授权委托书、输入签署人姓名／邮箱发起线上签署、审核启用四个动作；完成后自动刷新当前出租四阶段状态。
- 新增操作抽屉三语文案、表单间距与回归检查；流程相关测试 12/12 通过，Vite build 通过（保留既有 chunk size warning）。
- 使用者反馈上线后出现 HTTP 500；检查确认本机数据库 `leases` 表尚未执行 `rental_mandate_id` 迁移，后端新查询读取不存在的字段导致流程中心刷新失败。
- 已执行 `database/migrate_lease_rental_mandate.sql`，确认 `leases.rental_mandate_id` 已建立，类型为可空 BIGINT，并保留既有历史租约兼容性。
- 继续接入当前页操作抽屉：新增租客、建立关联当前出租委托的租约、生成并保存 OTR、生成租赁合约并发起签署、确认首期收款。
- 流程模型调整为“租客 → 租约 → OTR → 租赁合约”，支持新建租客后保留当前页上下文，再继续下一步，不需要重新从其他模块查找房产。
- 当前页操作接入 `createAdminTenant`、`createAdminLease`、合约记录保存、租赁合约签署与首期收款确认 API；前端测试 13/13 通过，Vite build 通过（保留既有 chunk size warning）。
- 使用者要求授權線上簽署視窗可直接輸入／修改簽署人 Email，避免只能使用原業主電郵或 prompt。
- 將簽署人姓名與 Email 放入授權簽署視窗，開啟時預填業主資料但允許改成其他簽署人；送出前驗證姓名與 Email，移除 prompt 輸入流程。
- 新增簽署人輸入回歸測試；完整租房流程相關測試 19/19 通過，Vite build 通過（保留既有 chunk size warning）。
- 使用者確認授權簽署視窗不需要「授權委託書」操作按鈕，只保留簽署人姓名、Email 與發起線上簽署動作。
- 移除授權簽署視窗底部的「授權委託書」按鈕，保留文件狀態與唯一的線上簽署入口；新增按鈕不存在回歸檢查。
- 租房流程相關測試 19/19 通過，Vite build 通過（保留既有 chunk size warning）。
- 使用者要求將簽署資訊放入同一個盒子，並把發起線上簽署按鈕放到姓名／Email 資訊下方。
- 將文件資訊、簽署人姓名／Email 與發起按鈕整合為單一垂直資訊盒，按鈕改為欄位下方排列。
- 新增資訊盒順序回歸檢查；租房流程相關測試 19/19 通過，Vite build 通過（保留既有 chunk size warning）。
- 使用者回饋簽署視窗格子過多；移除姓名／Email 內層盒子的邊框、背景與額外留白，保留外層資訊盒及輸入框。
- 租房流程相關測試 19/19 通過，Vite build 通過（保留既有 chunk size warning）。
- 使用者回報流程卡片「開始處理」只定位到大概的租賃管理頁，要求精準落到實際操作位置。
- 將 14 個步驟改為結構化精準目標：房產頁籤、委託工作區流程、租賃工作區流程，以及對應的照片／附件／交接清單／OTR／收支／維修頁籤。
- 新增路由參數處理：委託建立／授權／簽署／審核、租客建立、租約建立、租約合約簽署、帳單明細與指定房產租賃收支操作會自動開啟對應資料或表單。
- 新增流程目標與第一個缺失房產支援資料頁籤測試；租房流程相關測試 15/15 通過，Vite build 通過（保留既有 chunk size warning）。
- 使用者回報租房流程頁面內容貼近視窗與面板邊緣；定位為先前滿寬調整移除了主流程面板水平內距。
- 統一補回流程頁面左右外距、右側主面板 20px 內距與左側房產列表 16px 內距，保留內容滿寬但避免貼邊。
- 新增版面間距回歸測試；租房流程相關測試 16/16 通過，Vite build 通過（保留既有 chunk size warning）。

## 2026-07-30

- 使用者回報多建案／多房產操作時，流程跳轉不能寫死或跳到錯誤項目。
- 定位根因：房產工作區與房產詳情路由誤把 `ownerUnitId` 當成 `unitId` 使用；同一業主有多個建案時會載入或跳轉到錯誤房產。
- 修正為：房產工作區與詳情路由使用選中房產的 `unitId`；委託與房產附屬資料 API 繼續使用 `ownerUnitId`；租賃資料使用實際 `unitId`。
- 新增多房產識別碼回歸測試；租房流程相關測試 17/17 通過，Vite build 通過（保留既有 chunk size warning）。
- 進一步審計所有房產／合約／業主／租賃入口，發現合約管理中心的房產編輯跳轉仍誤用 `ownerUnitId`。
- 合約資料列補上實際 `unitId`，編輯房產改用 `unitId` 跳轉；新增合約中心識別碼回歸測試。
- 完整多房產流程相關測試 18/18 通過，Vite build 通過（保留既有 chunk size warning）。
- 使用者要求租房流程改為每頁 4 個步驟。
- 將流程分頁大小由 5 調整為 4，14 個步驟現在分為 4／4／4／2 四頁；同步更新未完成步驟預設頁定位與回歸測試。
- 本次租房流程與界面回歸測試 13/13 通過，Vite build 通過（保留既有 chunk size warning）。
- 使用者回報「完善房產資料」只顯示紅色「房產照片」缺項，畫面沒有處理入口。
- 定位根因：儲存邏輯會要求交接清單與房產照片，但表單只在「完成交房且已有當前委託」時渲染清單／照片／附件欄位；「完善房產資料」因此無法解決自身的阻塞。
- 已將交接清單、房產照片上傳、交接附件上傳移到兩種房產準備操作共用的表單區域；交房專用的鑰匙、門卡、水電表等欄位仍只在交房條件下顯示。
- 新增回歸測試確認照片與清單不再被交房條件模板隱藏；流程中心聚焦測試 14/14 通過，Vite build 通過。
- 使用者回饋交接清單畫面過度縱向展開、項目被擠在右半欄，版面難以操作。
- 定位根因：`.rental-action-wide` 未跨滿表單兩欄，且清單項目繼承普通表單的垂直排列樣式。
- 已將跨欄欄位設為滿寬；清單改為「項目名稱／數量」同列、固定最大高度獨立滾動，新增項目輸入固定在底部，並補上手機單欄規則。
- 流程中心聚焦測試 15/15 通過，Vite build 通過。
- 使用者要求不要在主表單直接展開交接清單，改用按鈕開啟彈窗，願意修改時再進入清單編輯。
- 主表單改為顯示交接清單摘要與「編輯清單」按鈕；新增交接清單彈窗，可修改數量／備註、添加清單項目，完成編輯後回到主表單再統一保存。
- 新增三語彈窗文案與關閉狀態管理；流程中心聚焦測試 16/16 通過，Vite build 通過。
- 使用者進一步要求突出「編輯交接清單」、拆開數量與備註、提供刪除按鈕，並讓新增項目入口一眼可見。
- 確認後端原先只有 `defaultQuantity` 欄位；新增獨立 `notes` 欄位，更新 DTO、Mapper、Service 與資料庫 schema／遷移，並已將本機資料庫欄位實際建立完成。
- 清單彈窗改為明確的「項目／數量／備註／刪除」編輯區；新增項目改為有標題、說明與主要按鈕的區塊；刪除先在本次編輯中暫存，外層保存時才提交，避免取消操作誤刪資料。
- 前端流程中心聚焦測試 17/17 通過，Vite build 通過；後端清單單元測試 3/3 通過，Maven `test-compile` 通過。
- 使用者回饋新增交接項目時看不出所屬部分，且清單過長會把「完成編輯」按鈕推到視窗外。
- 新增項目區加入「所屬部分」下拉選擇，新增資料會保存選定的 category；彈窗改為只讓清單內容區滾動，底部完成編輯區固定可見。
- 流程中心聚焦測試 18/18 通過，Vite build 通過。
- 使用者提供截圖確認新增區與彈窗底部仍需進一步收緊；將所屬部分選擇、項目名稱與新增按鈕統一為同一排，並將彈窗內容滾動與底部操作區完全分離。
- 最終流程中心聚焦測試 18/18 通過，Vite build 通過。
- 使用者回報 Spring Boot `testCompile` 編譯錯誤：`AdminPropertyWorkspaceServiceTest` 仍按舊版 11 參數建立 `AdminPropertyLeaseOptionResponse`。
- 根因是 DTO 新增 `rentalMandateId` 後記錄建構器需要 12 個參數；已在測試夾具補上對應的 `null` 值。
- 後端 Maven `-DskipTests test-compile` 驗證通過，確認測試原始碼已能通過編譯。
- 使用者回報交接清單「所屬部分」仍無法選擇，且新增區域版面不美觀。
- 根因是下拉選項原本依賴當前已載入清單；資料不完整時，`select` 的目前值可能沒有對應選項。改為固定標準分類目錄，並保留後端回傳的自訂分類。
- 新增區改為明確的「所屬部分／項目名稱／添加」三欄表單，每個欄位都有可見標籤，並固定預選「客廳 Living Room」。
- 流程中心聚焦測試 19/19 通過，Vite build 通過（保留既有 chunk size warning）。
- 使用者再次回報「所屬部分」下拉框被底部遮擋，點擊後無法選擇。
- 根因確認為原生 `select` 位於 `overflow:hidden` 的交接清單彈窗卡片底部，下拉選單展開區被容器裁切；這不是分類資料缺失。
- 移除該處原生下拉框，改為可控的自訂分類選單，向上展開並以較高層級顯示；選擇後直接回寫新增表單的分類，彈窗卡片允許選單溢出顯示。
- 新增遮擋回歸測試；流程中心聚焦測試 20/20 通過，Vite build 通過（保留既有 chunk size warning）。
- 使用者提供新截圖，確認分類按鈕雖然出現，但新增區仍可能受到清單滾動容器限制，視覺上也被壓縮。
- 將分類選單透過 Vue `Teleport` 移到頁面層，按實際按鈕位置以 fixed 定位，並在靠近底部時自動向上展開；選擇分類後清除選單定位狀態。
- 新增「分類選單移出滾動容器」回歸測試；流程中心聚焦測試 21/21 通過，Vite build 通過（保留既有 chunk size warning）。
- 使用者再次回報分類欄看得到但仍無法穩定選擇。
- 進一步檢查後，將分類控件從 `<label>` 巢狀結構改為獨立 `div + button`，以 `pointerdown.prevent.stop` 單次觸發選單，避免瀏覽器標籤預設激活行為干擾自訂選單。
- 新增直接按鈕互動回歸測試；流程中心聚焦測試 22/22 通過，Vite build 通過（保留既有 chunk size warning）。
- 使用者新截圖確認分類選單實際展開後只剩空白細框，選項沒有渲染。
- 根因改為菜單的 `v-for` 仍依賴運行時清單分類狀態；改用進入操作時建立的固定分類選項狀態，保證清單資料為空時仍有 8 個分類；補足菜單最小高度與選項行高。
- 新增固定分類選項回歸測試；流程中心聚焦測試 23/23 通過，Vite build 通過（保留既有 chunk size warning）。
- 使用者再次提供截圖，確認菜單高度正常但內容仍是空白。
- 為避免運行時分類狀態與 Teleport 循環渲染造成空菜單，將 8 個標準分類改為模板中的明確按鈕，每個按鈕直接呼叫分類回寫方法；保留固定分類目錄作為其他流程資料來源。
- 構建產物確認包含「鑰匙 Keys」與「主浴室 Master Bathroom」按鈕；流程中心完整測試 23/23 通過，Vite build 通過（保留既有 chunk size warning）。
- 使用者確認先前已做過「交接報告生成」，要求移除容易誤解的「交接附件」入口，回到報告生成流程。
- 流程中心交房表單改為「交接報告」操作區：先保存目前交接清單與照片，再呼叫既有交接報告 API，生成後自動下載報告，並顯示已生成報告數量與成功提示。
- 移除流程中心對 `handoverAttachmentFiles` 與一般房產附件上傳的依賴，避免把交接報告與普通附件混在一起；補上中／繁／英三語文案與報告操作回歸測試。
- 流程中心完整測試 24/24 通過，Vite build 通過。
- 使用者確認交接照片不應是必填；已將照片改為可選，仍保留交接清單為生成報告的必要資料。
- 同步放寬生成按鈕與保存校驗，避免沒有照片時被前端攔截；流程中心完整測試 25/25 通過，Vite build 通過。
- 使用者發現第一階段只要有房產圖片就會被判定完成，指出每次出租都必須存在本次出租的交接報告。
- 修正出租工作台判斷：当前委托存在时，第一阶段必须有归属于该委托的交接报告；报告通过 `contentJson.mandateId` 关联当前出租，历史委托报告不会顶替。
- 入住与收款阶段也要求当前出租交接报告与实际交接完成；新增「本次出租交接报告」缺项与阻塞文案，并让流程中心生成报告时写入当前委托 ID。
- 新增回归测试覆盖「有图片但无当前交接报告」场景；流程中心完整测试 26/26 通過，Vite build 通過。
- 使用者回報選擇交接照片後，點擊生成交接報告仍顯示「操作失敗」。
- 根因是流程中心上傳物件照片時傳入後端不存在的 `category=handover` 與 `rentalStage=handover`；後端只接受既有照片分類，以及租賃階段 `before/after`。
- 已改用後端支援的 `category=other`，有當前租約時使用 `rentalStage=after`，沒有租約時不傳租賃階段；新增回歸測試，前端完整測試 27/27 通過，Vite build 通過。
- 使用者要求交接照片按報告模組歸類、每次出租隔離且可沿用歷史物件照片，避免直接覆蓋。
- 流程中心新增照片所屬模組選擇，使用與 PDF 生成器一致的章節名稱；生成報告時把新照片與沿用照片按選定模組寫入 `photoMeta`，照片會出現在對應章節。
- 新增「沿用物件照片」勾選區：原有物件照片會下載為本次報告副本，原照片不被修改；有當前租約時新照片按租約保存，沒有租約時只進入交接報告，不誤存為永久物件照片。
- 同步載入當前租約照片、增加中／繁／英文案與響應式樣式；前端完整測試 28/28 通過，Vite build 通過。
- 使用者回饋交接照片選擇器只能看見單一檔案，沿用物件照片缺少縮略圖與所属板块信息。
- 新照片選擇改為可一次加入多張，建立逐張照片卡片：每張顯示縮略圖、檔名、所属板块下拉選擇與移除按鈕；報告生成會保留每張照片自己的模組。
- 沿用物件照片改為縮略圖卡片，顯示檔名與可讀的照片分類／交接模組；勾選後仍複製到本次報告，不修改原物件照片。
- 新增前端回歸測試；照片流程測試 24/24 通過，Vite build 通過（保留既有 chunk size warning）。
- 使用者回報縮略圖仍未顯示；定位到沿用照片直接以內容 API URL 作為 `<img>`，失敗時無法使用 API 的登入憑證，且未利用照片自身的 `ownerUnitId`。
- 新增帶 `credentials: include` 的 Blob 縮略圖回退，成功後轉成 `URL.createObjectURL`；照片網址優先使用照片自身的房產單位 ID。
- 新增縮略圖認證回退測試；流程中心照片測試 25/25 通過，Vite build 通過（保留既有 chunk size warning）。
- 使用者要求已完成的出租準備仍可重新操作；已完成階段保留原按鈕位置並改顯示「重新完成」，點擊後重新開啟原出租準備表單。
- 將流程中心更名為「租房操作中心」，移除流程頁的「查看歷史記錄」文字與跳轉。
- 將入口改為沿用既有流程彈窗樣式的「查看之前生成的文件」，在當前頁列出授权委托书與交接报告並直接下載，不跳轉其他頁面。
- 新增重新完成與文件查看回歸測試；聚焦測試 31/31 通過，Vite build 通過（保留既有 chunk size warning）。
- 使用者要求「查看之前生成的文件」不要與主要操作搶視野；已改為白底描邊、較小尺寸的二級按鈕，保留「重新完成」作為唯一主按鈕。
- 新增二級按鈕樣式回歸測試；流程中心測試 27/27 通過，Vite build 通過（保留既有 chunk size warning）。
- 使用者回報二級按鈕視覺仍未改變；確認通用 `.rental-stage-actions button` 的 CSS 優先級覆蓋了二級按鈕樣式。
- 將查看文件按鈕改用 `.rental-stage-actions .generated-files-link` 高優先級選擇器，確保白底描邊樣式真正生效；測試 27/27 通過，Vite build 通過。
- 使用者回報點擊保存後新照片沒有回傳物件照片。
- 根因是保存邏輯只在存在當前租約時才呼叫物件照片 API；已改為無論是否已有租約都保存到物件照片，若有租約再附加 `leaseId` 與 `rentalStage=after`，並保留每張照片的所属模块描述。
- 新增「無租約仍保存物件照片」回歸測試；流程中心與工作台測試 33/33 通過，Vite build 通過。
- 使用者要求物件照片分類也能選擇交接報告所需的鑰匙／通行卡、客廳、飯廳、廚房、主臥室、主浴室及兩類瑕疵。
- 物件照片新增分類下拉選項，後端照片分類白名單同步放行 8 個交接分類；流程中心同步將這些分類映射到 PDF 對應章節。
- 新增照片分類回歸測試；相關前端測試 35/35 通過，Vite build 通過；後端 Maven 測試因目前環境 `JAVA_HOME` 未正確設定未能執行。
- 使用者回報已完成業主線上簽署，但「委托与授权」仍顯示被阻塞。
- 根因是流程中心載入的是簽署前的舊委托文件狀態；原「查看签署状态」動作沒有先重新載入當前委托文件，會直接再次發起簽署。
- 現在進入查看簽署狀態會先刷新工作台，偵測到 `signed_contract`／`authorization_signed` 後不再重發簽署；頁面重新取得焦點時也會刷新，並加入卸載清理。
- 新增授權狀態刷新回歸測試；工作台與租房流程測試 34/34 通過，Vite build 通過。
- 進一步核對後端真實簽署流程：已簽授權文件原本只以 `relationType=authorization` 回傳，雖然 `documentType=signed_contract`，前端因此漏判為已簽署。
- 前端新增對「authorization + signed_contract」舊資料的兼容判定；後端新簽署文件改以 `relationType=signed_contract` 建立委托關聯，避免再次被判定為阻塞。
- 新增後端 SQL 回歸檢查；前端工作台與租房流程測試 35/35 通過，Vite build 通過。
- 後端 SQL 測試已加入但目前工作環境未設定 `JAVA_HOME`，Maven 無法啟動；已另外以靜態檢查確認簽署關聯值已改為 `signed_contract`。
- 使用者確認「查看之前生成的文件」只需要显示当前阶段相关文件，不应列出整套房产的全部文件。
- 新增阶段文件筛选工具：出租准备只显示当前出租交接报告；委托与授权只显示当前委托文件；招租与签约只显示当前租约 OTR／租赁合约；入住与收款只显示当前租约交接报告、收款收据和收款凭证。
- 所有文件先按当前委托／当前租约过滤，旧委托和旧租约文件不会混入；同时接通相应下载接口。
- 新增阶段文件筛选与弹窗接线测试；相关测试 37/37 通过，Vite build 通过。
- 使用者回报已生成交接报告，但出租准备仍显示“本次出租尚未生成交接报告”。
- 根因是历史版本生成的报告可能只有房产范围和创建时间，没有 `contentJson.mandateId`；严格按委托 ID 筛选时，报告数量存在但不能归入当前出租。
- 新增兼容判定：明确绑定当前委托的报告优先；对没有绑定字段但创建时间不早于当前委托创建时间的报告，归入当前出租，同时保留旧委托报告隔离规则；生成文件列表同步使用同一判定。
- 新增回归测试复现并覆盖该场景；相关测试 38/38 通过，Vite build 通过。
- 使用者回报点击「审核并启用委托」仍显示操作失败。
- 核对发现审核后端原本只统计 `authorization` 关联，或必须同时存在电子签署请求；前端已识别的有效 `signed_contract` 文件在部分旧／手动签署数据下会被后端漏掉。
- 审核 SQL 现在同时接受当前委托下未作废的 `signed_contract` 文件，前端补充审核错误文案；保留当前委托范围限制。
- 相关前端测试 38/38 通过，Vite build 通过；既有授权工作区结构测试仍有一条历史断言与当前代码不一致，未纳入本次通过统计；Maven 仍因 `JAVA_HOME` 未设置无法执行。
- 使用者指出「招租与签约」不应强制新增租客，已有租客应可直接沿用。
- 流程中心新增现有租客选项加载与下拉选择；选择已有租客只设置当前租客 ID，不调用新增租客接口；未选择时保留原新增租客表单。
- 提交按钮会根据选择显示「使用此租客」，沿用后自动进入建立租约阶段；相关测试 31/31 通过，Vite build 通过。
- 使用者回報「建立租約」填寫後只顯示通用操作失敗。
- 查詢本機資料庫並重現截圖資料後確認：当前出租委托只覆盖至 `2026-07-31`，表單提交的租約結束日為 `2027-07-31`，後端因此拒絕「当前出租委托未覆盖完整租期」；前端原本把這個英文原因統一成通用錯誤。
- 建立租約表單現在會以當前委托的起訖日限制日期選擇，並顯示委托期限提示；提交前增加租期覆蓋校驗，後端同類錯誤也補上可讀中文文案。
- 新增租期校驗回歸測試：超出委托期限的場景會失敗、完整落在委托期限內的場景會通過；相關測試 33/33 通過，Vite build 通過。
- 使用者回報「生成 OTR 出价函」HTTP 500。
- 实际调用确认 PDF 模板生成本身成功，500 出在保存合同记录；数据库已有旧的 `OTR-12` 记录但 `lease_id` 为空，重新插入时触发房产合同编号唯一约束。
- OTR 记录现在按当前租约与合同类型查找：同一租约重新生成会更新原文件，新租约使用新的租约 ID／OTR，不会复用或覆盖其他出租周期；同时兼容修复旧的未关联 OTR 记录。
- 新增 Mapper SQL 回归检查，后端 Maven 因本机依赖仓库无法下载暂未完成编译；待重启 Spring Boot 后验证接口。
- 使用者回报签署／审批完成后仍显示缺少资料，且未完成的租赁合约曾提前显示完成。
- 核对当前数据库确认授权签署文件和租赁合约签署请求都已是 signed；根因一是完成阶段仍无条件显示最后一个缺少项，根因二是租约列表把所有已有租约伪装成已签 L.租赁合约。
- 流程状态现在只有真正完成时才显示空缺少资料；委托激活但缺签署资料不会显示勾；租赁合约状态改为读取电子签署请求，未签署时保持处理中。
- 页面回到前台时会刷新整个当前出租工作台，不再只刷新委托授权阶段，确保线上签署回调后授权和租赁合约状态同步。
- 新增流程状态与回调回归测试；相关前端测试 41/41 通过，Vite build 通过。
- 后端 Mapper SQL 回归测试 3/3 通过；重新编译并重启 Spring Boot 后，实际工作台返回当前租约合约 `status=completed`、`leaseContractMissing=false`。
- 实际重复生成当前租约 OTR 已由 HTTP 500 改为 HTTP 200，并正确关联 `leaseId=12`；旧未关联记录已兼容修复。
- 使用者回報「入住与首期收款」阶段也不正确。
- 根因一：租金帳單接口是按当前租约返回，响应字段是 `invoiceId` 且没有 `leaseId`；前端按 `id`／`leaseId`再次筛选，导致已有首期账单被当成不存在。
- 根因二：确认收款动作错误要求 `proofDocumentId`，但后端收款确认的凭证上传是可选项；已收款且存在确认记录即可完成首期收款阶段。
- 根因三：工作台没有加载当前委托的交接完成记录，只加载交接报告；即使后端交接返回 `status=completed`，前端仍显示缺少入住。
- 前端已按当前委托选择租约，给租约接口数据补齐 `id`、`invoiceId`、`leaseId`，确认收款使用 `invoiceId`，载入并传入当前委托的交接完成记录；新增回归测试后租房流程与工作台测试 45/45 通过。
- 后端按日分摊金额现在不会因四舍五入产生 RM0.00；已有未收款零金额首期账单可由重新生成首期账单动作修复，月度自动生成 SQL 也同步设置最低 RM0.01。
- 新增后端回归测试覆盖零金额分摊与既有零金额账单修复；`AdminTenancyServiceTest` 16/16 通过，后端重新编译 275 个源文件成功，Spring Boot 已重启。
- 已对当前测试出租资料执行一次重新生成首期账单：HTTP 204，账单从 RM0.00 修复为 RM0.01；目前流程正确停在“等待首期收款”，不会误显示完成。
- 前端最终测试 45/45 通过，Vite build 通过（保留既有 chunk size warning）。
- 使用者确认四阶段之后是否还有流程；已明确当前出租主流程只包含四阶段，签署与首期收款完成后进入独立的日常运营模块，不再增加第五个出租完成阶段。
- 使用者进一步明确租约生命周期：入住交接完成即结束出租主流程；日常收租独立运行；正常租期届满且租金／押金／账单结清、转租或提前退租时，结束原租约，再生成归属于原租约的退租交接报告。
- 领域词汇已记录于 `CONTEXT.md`：区分当前出租周期、入住交接、日常运营、结束租约和退租交接报告；后续不得用入住交接报告代替退租交接报告，也不得让收租状态阻塞出租主流程。
- 2026-07-30：按确认后的租约生命周期完成收尾：出租主流程四阶段最后改为“入住交接”，不再受首期账单、收款或付款凭证阻塞；收租与账单保留在独立“日常运营”区。新增结束租约操作（正常届满／提前退租），后端校验结束日期、记录审计、清理未来未付款账单，并在无其他 active 租约时恢复房产可出租状态。
- 2026-07-30：新增“退租交接报告”操作，报告内容携带原租约 `leaseId` 与 `handoverType=move_out`；转租后即使新租约已 active，旧租约的退租交接仍会显示并可完成，已结束的委托过期后也可继续补退租报告。四阶段生成文件只显示本次出租需要的交接文件，不再混入收据和付款凭证。
- 2026-07-30：验证完成：前端相关回归测试 54/54、Vite production build 成功；后端 `AdminTenancyServiceTest` 17/17 成功。Vite 仍有既有的大 chunk size warning，但不影响构建成功。
- 2026-07-30：使用者选择将「租房操作中心」更名为「租赁智控台」；已同步简体中文、繁体中文、英文、侧边栏和仪表盘入口文案，流程相关标题统一改为「租赁流程」。
- 2026-07-30：按使用者确认先只做界面：日常运营改为单一「前往运营中心」入口，点击打开宽屏二级运营面板；面板包含运营概览、收租与账单、维修工单、租约事件四个分页，暂不接新增费用和维修工单后端接口，保留现有首期收款、结束租约、退租交接入口。前端流程中心测试 38/38 通过，Vite build 成功。
- 2026-07-31：开始设计日常运营后端实现。确认现有 `rent_invoices`、`rent_payments`、`cashflow_entries`、`maintenance_work_orders` 已能提供基础能力，但月度账单目前只有总金额，无法清晰承载租金、管理费、水电费、维修转收费等明细；下一步应增加租金账单明细，并让运营记录全部绑定当前 `lease_id`，通过一个运营工作台聚合模块提供按月份的概览、账单、工单和租约事件。
- 2026-07-31：完成日常运营第一版接入：新增按当前房产和 active lease 查询的运营快照接口，按运营月份返回当月账单、账单明细和当前租约维修工单；新增运营费用写入（管理费、水电费、维修转收费、其他费用）并同步增加当月账单应收金额；新增当前租约维修工单建立、状态历史和审计记录。
- 2026-07-31：运营中心二级界面已接入真实数据：可在面板内切换月份、建立月账单、确认收款、添加杂费、建立维修工单；概览改用真实应收／已收／未收和当前租约工单数量，旧租约数据不会作为当前运营数据回退显示。
- 2026-07-31：新增 `database/migrate_rental_operations.sql` 并已应用到本机 `ccps_property_management` 数据库；确认 `rent_invoice_items`、`cashflow_entries.lease_id`、`maintenance_work_orders.lease_id` 均已存在。前端运营流程测试 40/40、后端运营 Mapper SQL 2/2、运营服务 4/4、租约服务 17/17 通过，Vite production build 和 Spring Boot compile 通过；保留既有 Vite chunk size warning。
- 2026-07-31：完成最终界面收口：月账单、确认收款、杂费和维修工单都在运营中心内处理；账单明细与租约事件中的未实现按钮不再伪装成可操作入口。最终租房流程相关前端测试 57/57 通过，Vite production build 再次成功。
