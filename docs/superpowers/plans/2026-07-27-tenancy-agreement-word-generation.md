# 租赁合约 Word 生成实施计划

## 任务 1：导入并锁定原始模板
- 将用户提供的 `20260413 CCPS KL Owner Tenancy Agreement [Template].docx` 放入后端 classpath。
- 保留模板内部段落、表格、图片和样式，仅在生成副本时替换字段。
- 增加测试，确认模板包含原有 21 个表格。

## 任务 2：后端生成服务与下载接口
- 新增租赁合约 Word 生成服务，读取原始模板并填入案件编号、双方资料、单位地址、租期、租金、押金和备注。
- 通过现有合约模板接口增加 `tenancy-agreement` 类型，返回 `.docx` 下载文件。
- 增加单元测试，确认字段被替换、模板结构保留、文件名清晰。

## 任务 3：前端入口与资料预填
- 在租客与租金的租约详情操作区增加“生成租赁合约”入口。
- 以当前租约自动带入已存在的房东、租客、单位、租期、租金、押金资料。
- 对缺少的身份证、地址、付款日等资料保留可编辑输入，并提供下载 Word 按钮。
- 接入简体、繁体和英文文字。

## 任务 4：验证
- 执行后端租赁合约与既有合约模板测试。
- 执行前端相关测试和生产构建。
- 检查输出 Word 的 OOXML 结构、字段内容和原始模板未被修改。

## 执行结果
- 已完成原始模板导入、后端 Word 生成接口、租约详情入口、三语文字和自动预填。
- 后端测试、前端测试和生产构建已通过；Word 结构测试确认保留 21 个原始表格。
