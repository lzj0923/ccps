# ADR 0001：系统财务通知采用固定规则

状态：已接受

租金催缴、买房分期款到期和预备金不足均采用系统内置规则自动执行，不复用可新增、编辑或删除的普通提醒规则。这样可以防止关键财务节点因误删、改错天数或停用规则而漏发，同时仍在统一通知历史中保留发送渠道、结果与审计记录。该自动化只负责判断业务状态并发送通知，不自动扣款、入账、确认收款、结束租约或停用门禁；涉及资金和合同状态的最终操作继续由工作人员依据凭证确认。
